// SCAFFOLDED module — schema models exist; services/APIs land in a later milestone.
// When implementing, expose the public surface here and keep cross-module access
// through service interfaces only (see CONVENTIONS.md "how to add a new module").
export const __scaffolded = "inventory" as const;
